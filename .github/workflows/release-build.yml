name: Build Release Binaries

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build (for example v1.0.1)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y docker.io git tar gzip zip rsync

      - name: Start Docker
        run: |
          sudo systemctl start docker
          sudo chmod 666 /var/run/docker.sock
          docker info

      - name: Install Berkeley DB 4.8
        run: ./contrib/install_db4.sh "$(pwd)"

      - name: Resolve build tag
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag }}"
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Build release artifacts
        run: ./contrib/build_release.sh "${{ steps.vars.outputs.tag }}"

      - name: Upload release-builds artifact
        uses: actions/upload-artifact@v4
        with:
          name: rincoin-${{ steps.vars.outputs.tag }}-release-builds
          path: release-builds/
          if-no-files-found: error

      - name: Publish GitHub Release assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.vars.outputs.tag }}
          files: |
            release-builds/*/tarballs/*
            release-builds/*/source/SHA256SUMS.txt
            release-builds/*/tarballs/SHA256SUMS.txt
